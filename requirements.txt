# GUI应用依赖（使用Python内置库）
# 无需额外依赖运行gui.py

# API服务器依赖（可选）
flask>=2.3.2
flask-cors>=5.0.1
requests>=2.32.4
pyOpenSSL>=23.3.0  # 用于HTTPS支持
python-dotenv>=1.0.0  # 用于环境变量管理

# 开发和测试依赖（可选）
pytest>=6.0.0
pytest-cov>=2.10.0 